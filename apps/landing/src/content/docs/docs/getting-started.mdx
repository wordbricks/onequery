---
title: Getting Started
description: Install OneQuery, connect one source, and run the first governed query.
sidebar:
  order: 2
---

import { Steps } from "@astrojs/starlight/components";

This guide gets one source working from the CLI. OneQuery keeps credentials server-side; callers use source identifiers like `postgres://warehouse`.

## Prerequisites

For the default path:

- Node.js 22+
- A read-only credential or limited provider token for one source

## Install the CLI

```sh
npm install -g @onequery/cli
```

Use [Install the CLI](/docs/guide/install-cli/) for Homebrew, Bun, install script, or no-install commands.

## Choose a Server

| Situation                                  | Choose this path                                        |
| ------------------------------------------ | ------------------------------------------------------- |
| Your team gave you a OneQuery server URL   | Set that URL and continue.                              |
| You are evaluating OneQuery alone          | Start a local gateway.                                  |
| You are rolling out a private team gateway | Read [Self-Host OneQuery](/docs/operations/self-host/). |

Set a shared server URL when you have one:

```sh
onequery config set api.server_url https://onequery.example.com
```

Start a gateway only when you run OneQuery yourself:

```sh
onequery gateway start
```

See [Choose Server Path](/docs/guide/start-gateway/) for details.

## First Run

<Steps>

1. Verify and log in.

   ```sh
   onequery --version
   onequery auth login
   onequery auth whoami
   ```

2. Connect a source.

   Replace the placeholders with read-only PostgreSQL credentials.

   ```sh
   onequery source connect --source postgres \
     --input '{"sourceKey":"warehouse","credentials":{"host":"db.example.com","port":5432,"database":"app","username":"onequery_readonly","password":"<read-only-password>","sslMode":"require"}}'
   ```

3. Run a smoke query.

   ```sh
   onequery query exec --source postgres://warehouse --sql "select 1"
   ```

4. Confirm the source.

   ```sh
   onequery source list
   ```

</Steps>

You are done when:

- `onequery auth whoami` shows the expected user and organization.
- `onequery source list` shows the source you connected.
- The smoke query returns one row.

## What to Read Next

- [Source identifiers](/docs/concepts/source-identifiers/) explains `postgres://warehouse` and other source URI shapes.
- [Run a query](/docs/guide/run-query/) covers read-only query execution.
- [Agent workflows](/docs/guide/agent-workflows/) shows how to expose OneQuery to coding agents and automation.
