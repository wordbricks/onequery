version: v2
modules:
  - path: .
deps:
  - buf.build/bufbuild/protovalidate
  - buf.build/googleapis/googleapis
lint:
  use:
    - STANDARD
breaking:
  # TODO: Wire `buf breaking` into proto:check/CI after the internal-only
  # provider catalog wire change settles.
  use:
    - FILE
