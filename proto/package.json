{
  "name": "@onequery/proto",
  "private": true,
  "scripts": {
    "format": "buf format -w .",
    "lint": "buf lint .",
    "generate": "buf generate -o .. .",
    "check": "bun run lint && bun run generate && git diff --exit-code -- ../packages/cli-server/src/connect/gen"
  },
  "devDependencies": {
    "@bufbuild/buf": "catalog:",
    "@bufbuild/protoc-gen-es": "catalog:"
  }
}
