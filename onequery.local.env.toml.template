# Generated from packages/dev-config/src/local-env.ts via `bun run env:sync`.
# Managed local-development values live here.
# Edit this TOML file directly; process env is only for explicit overrides at
# command launch time.
# === Core Runtime ===
# Baseline local runtime values needed for the OSS web, server, and DB loop.

# DATABASE_URL: Local Postgres connection string for development and Drizzle.
# consumers: web, server
DATABASE_URL = "postgres://onequery:onequery@localhost:5454/onequery"

# BETTER_AUTH_URL: Base URL used for Better Auth callbacks and links.
# consumers: web, server
BETTER_AUTH_URL = "http://localhost:4615"

# WEB_URL: Public web origin used for local callbacks and asset links.
# consumers: web, server
WEB_URL = "http://localhost:4615"

# BETTER_AUTH_SECRET: Development-only Better Auth signing key. Seeded randomly when missing.
# consumers: web, server
BETTER_AUTH_SECRET = "generated-by-config-sync"

# CONNECTOR_ENROLLMENT_TOKEN: Shared enrollment token for connector registration in local dev.
# consumers: web, server
CONNECTOR_ENROLLMENT_TOKEN = "dev-connector-token"

# MASTER_ENCRYPTION_KEY: Sample placeholder for the key used to encrypt stored credentials.
# consumers: web, server
MASTER_ENCRYPTION_KEY = "sample-encryption-key"

# DISABLE_RATE_LIMIT: Disable API rate limiting for local development and e2e.
# consumers: web, server
DISABLE_RATE_LIMIT = true
