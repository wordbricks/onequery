{
  "name": "@onequery/dev-config",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "exports": {},
  "scripts": {
    "build": "bun build ./src/master-encryption-key.ts --outdir ./dist --target node --format esm --packages external --root ./src --entry-naming '[name].js'",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
