{
  "name": "@onequery/config-loader",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outfile ./dist/index.js --target node --format esm --packages external",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "smol-toml": "1.6.1",
    "zod": "catalog:",
    "zod-config": "1.4.0"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
