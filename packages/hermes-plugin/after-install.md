# OneQuery Plugin Installed

The OneQuery Hermes plugin is installed.

Before using internal data tools, confirm the OneQuery CLI is authenticated:

```bash
onequery auth whoami
onequery org current
```

Then start Hermes and run:

```text
/onequery status
```

For internal data analysis, load the bundled skill:

```text
Load skill onequery:internal-data-analysis.
```

