# To prevent merge-conflict between staged/unstaged files,
# use `pre-push` instead of `pre-commit`.
# But it does not provide `stage_fixed` option.
pre-push:
  commands:
    check:
      run: turbo check:fix
