pre-commit:
  commands:
    lint:
      run: bunx oxlint --fix {staged_files}
      stage_fixed: true
    format:
      run: bunx oxfmt {staged_files}
      stage_fixed: true

pre-push:
  commands:
    lint-full:
      run: bunx turbo lint -- --type-aware --type-check
