We treat workflows as explicit, deterministic state machines.
- State transitions define truth.
- Reducers are pure.
- Effects are deferred and isolated via async dispatch.
- Failure and retry are modeled as normal lifecycle transitions, not exceptions.

If you encounter something surprising or confusing in this project, flag it as a comment.

- Runtime: Bun
- For type checking, use `bun run typecheck`.
- For testing, use `bun run test` instead of `bun test`.
- For linting, use `bun run lint --format json`. For type-aware lint, append `--type-aware` flag (takes more time then normal lint).
- For checking lint/format for whole monorepo, use `turbo run check`, to fix, `turbo run check:fix`

### Auto-generated files and how to generate them:
NOTE: Don't edit them manually.
- `apps/web/src/routeTree.gen.ts` - Generated by TanStack Router
- `packages/ui/src/components/ui/*` - Generated by shadcn/ui CLI
- `packages/db/src/migrations/*` - Generated by Drizzle CLI
