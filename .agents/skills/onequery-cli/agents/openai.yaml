interface:
  display_name: "OneQuery CLI"
  short_description: "Inspect OneQuery-connected company data"
  default_prompt: "Use $onequery-cli to inspect OneQuery-connected company data, choose the right org and source, run a read-only query, and summarize the evidence."
